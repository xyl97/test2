server:
  port: 8078
  servlet:
    session:
      timeout: 3s
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/hungli
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root


  main:
     allow-circular-references: true

